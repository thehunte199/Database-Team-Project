CREATE TABLE donation (
  donationID int NOT NULL AUTO_INCREMENT,
    dType varchar(20),
      receivedDate DATE,
        amount int,
          donor varchar(40), 
            reason varchar(255),
              CONSTRAINT PRIMARY KEY (donationID)
);
